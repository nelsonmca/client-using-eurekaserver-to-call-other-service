# client-using-eurekaserver-to-call-other-service


simple client to access the service exposed through discovery service

start the app in sequence
-> start discovery-server app
-> start service app
-> start client app

URL: http://localhost:8080